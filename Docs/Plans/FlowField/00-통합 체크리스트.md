# Flow Field 전환 — 통합 체크리스트

## Context

벽(Wall) 물리 콜라이더(radius=0.4)와 그리드 점유 영역(2x2=2.0x2.0) 사이 불일치로 인해 Tank/EnemyBig 등 대형 유닛이 벽 사이 갭을 물리적으로 통과하는 버그가 발생. 근본 원인은 NavMesh/Grid/Physics 3중 데이터 소스의 불일치이며, NavMeshObstacle(managed GameObject)이 DOTS 패러다임에도 맞지 않음. 그리드를 경로 탐색의 단일 진실 소스로 만들고, Flow Field로 전환하여 다수 유닛 이동 성능도 확보한다.

**기획 원칙**: 유닛 간 밀기(displacement) 불가 — hardPush는 이미 제거됨 (soft separation만 사용).

---

## 설계 결정 요약

| 항목 | 결정 | 근거 |
|------|------|------|
| 알고리즘 | **Flow Field** (BFS 기반) | 목적지당 1회 계산, 다수 유닛 공유, RTS에 최적 |
| 유닛 크기 | 2종 passability 맵 (Small/Large) | Small(패딩 0)은 좁은 통로 통과, Large(패딩 1)는 벽 갭 차단 |
| 경로 스무딩 | 불필요 | Flow Field는 셀 단위로 방향을 제공, 자연스러운 곡선 이동 |
| 벽 충돌 | PredictedMovementSystem 기존 Physics 로직 유지 | 서브셀 정밀도 필요, 경로만 Grid로 전환 |
| Flying 유닛 | 장애물 무시 직선 이동 (동작 변경) | Flow Field 조회 스킵, 목적지 방향으로 직진 |
| 동적 장애물 | GridCell 직접 참조 + 캐시 무효화 | 건물 건설/파괴 시 모든 캐시 필드 폐기 |
| 하위 호환 | MovementWaypoints 유지 | FlowFieldSteeringSystem이 매 프레임 가상 웨이포인트 주입 |

**Small**: Hero, Worker, Striker, Archer, EnemySmall, EnemyFlying
**Large**: Tank, EnemyBig

---

## Flow Field 동작 원리

```
1. 유닛이 MoveRequestRpc 수신 → MovementGoal.Destination 설정, IsPathDirty=true

2. FlowFieldSystem (매 프레임):
   ├─ Phase 1: IsPathDirty=true인 유닛 수집 → 고유 목적지 셀 목록 추출
   ├─ Phase 2: 캐시 미스인 목적지에 대해 BFS 계산 (병렬 IJob)
   │   └─ BFS: 목적지 셀에서 출발, 인접 passable 셀로 확산, 각 셀에 방향(byte) 기록
   └─ Phase 3: 유닛에 FlowFieldRef(목적지 셀 키) 할당, IsPathDirty=false

3. FlowFieldSteeringSystem (매 프레임, 이동 중 유닛):
   ├─ 유닛의 현재 셀 → Flow Field에서 방향 조회
   ├─ 방향(byte) → 다음 셀 오프셋 변환 → 다음 셀 중심 월드 좌표 계산
   ├─ [중간 셀] Current = 다음 셀 중심, HasNext = true
   ├─ [목적지 셀] Current = MovementGoal.Destination (실제 월드 좌표), HasNext = false
   └─ PredictedMovementSystem이 기존대로 Current를 향해 이동

4. 도착: MovementArrivalSystem이 기존 로직으로 판정 (HasNext=false && 거리 < ArrivalRadius)
```

**캐시 전략**:
- Flat `NativeArray<byte>` 풀 (`maxFields * gridCellCount`) + `NativeHashMap<int, int>` (destinationKey → poolIndex, 메인 스레드 전용)
- 유닛 크기별 2개 캐시 (Small/Large), 각각 별도 풀
- 최대 32 필드 캐시 (32 * 10KB = 320KB, 100x100 그리드 기준)
- LRU 방식 필드 교체
- 그리드 변경 시 전체 캐시 무효화 (BFS ~0.1ms로 재계산 비용 무시 가능)
- passability 스냅샷은 이전 프레임 LateSimulation(GridOccupancyEventSystem) 결과

---

## Phase 체크리스트

### Phase 0: Grid 유틸리티 확장
- [ ] `GridUtility.CellCenterToWorld` 구현 (float2→float3 변환, Y=0)
- [ ] `GridUtility.IsPassable` 구현 (경계 밖 = blocked)
- [ ] `GridUtility.IsPassableForSize` 구현
- [ ] `GridUtility.BuildPassabilityMap` 구현
- [ ] 루프 없는 메서드에 `[MethodImpl(AggressiveInlining)]` 적용, 모든 메서드 `public`
→ 상세: [01-Phase0-Grid유틸리티.md](01-Phase0-Grid유틸리티.md)

### Phase 1: Flow Field 코어 알고리즘
- [ ] `FlowFieldCore` struct 구현 (`Shared/Utilities/` 배치, BFS, 8방향, 코너 차단)
- [ ] 방향 인코딩 (0-7, 255=None) + 방향→오프셋 변환 테이블
- [ ] BFS 방향 기록: 확산 역방향 기록
- [ ] `ComputeField`에 `[MethodImpl(AggressiveInlining)]` (struct 파라미터로 개별 `[BurstCompile]` 불가)
- [ ] 워커 메모리 Persistent 할당 (8세트, FlowFieldSystem 소유)
→ 상세: [02-Phase1-FlowField코어.md](02-Phase1-FlowField코어.md)

### Phase 2: FlowFieldSystem (PathfindingSystem 대체)
- [ ] `FlowFieldCacheData` 싱글톤 구현 (`IsGridStale`에 `[MarshalAs(UnmanagedType.U1)]`)
- [ ] LRU 추적 필드 (`LastUsedFrame`) + 교체 로직
- [ ] Collect Phase (메인 스레드): dirty 유닛 수집, 목적지 셀 추출
- [ ] Compute Phase (병렬 IJob): BFS 계산
- [ ] Apply Phase (메인 스레드): FlowFieldRef 할당, Partial Path 판정
- [ ] Flying 유닛 직선 이동 처리
- [ ] 시스템 의존성 설정 (`UpdateInGroup`, `WorldSystemFilter`, `UpdateAfter`)
- [ ] `OnDestroy`에서 FlowFieldCacheData + 워커 메모리 전체 Dispose
→ 상세: [03-Phase2-FlowFieldSystem.md](03-Phase2-FlowFieldSystem.md)

### Phase 2.5: FlowFieldSteeringSystem (PathFollowSystem 대체)
- [ ] 쿼리에 `WithNone<FlyingTag>` 필터 추가
- [ ] `FlowFieldRef.Key == -1` 스킵 로직
- [ ] Flow Field 방향 조회 → MovementWaypoints 주입
- [ ] 목적지 셀 판정 (HasNext=false)
- [ ] 중간 셀 2단계 look-ahead
- [ ] 캐시 미스 시 lazy re-pathing
- [ ] 시스템 attribute (`UpdateInGroup`, `WorldSystemFilter`, `UpdateAfter`, `UpdateBefore`)
→ 상세: [04-Phase2.5-SteeringSystem.md](04-Phase2.5-SteeringSystem.md)

### Phase 3: NavMesh 장애물 시스템 대체
- [ ] `GridObstacleResponseSystem` 구현 (시스템 attribute, NeedsNavMeshObstacle 비활성화 포함)
- [ ] `GridObstacleCleanup` ICleanupComponentData 도입
- [ ] `GridObstacleCleanupSystem` 구현 (시스템 attribute 포함)
- [ ] passability 타이밍 1프레임 스킵 메커니즘
- [ ] `MovementGoal.IsPathDirty/IsPathPartial`에 `[MarshalAs(UnmanagedType.U1)]` **추가** (ISystem Burst 필수)
→ 상세: [05-Phase3-장애물시스템.md](05-Phase3-장애물시스템.md)

### Phase 4: Authoring/컴포넌트 정리
- [ ] `MovementAuthoring` 변경 (AgentTypeIndex → PathfindingSize)
- [ ] 신규 컴포넌트 3종 추가 (GridPathfindingSize, FlowFieldRef, GridObstacleCleanup)
- [ ] NavMesh 관련 파일 제거 (NavMeshAgentConfig, PathWaypoint, NavMeshPathUtils)
- [ ] CurrentWaypointIndex 참조 제거 (6개 시스템, 7곳 — RangedAttackSystem 2곳)
- [ ] NavMeshObstacleReference managed class 제거
- [ ] StructureAuthoring, ResourceNodeAuthoring, StructureFootprint 주석 갱신
→ 상세: [06-Phase4-컴포넌트정리.md](06-Phase4-컴포넌트정리.md)

### Phase 5: 테스트
- [ ] ECSTestBase 확장 (Grid 싱글톤, 이동 유닛, FlowFieldCacheData 헬퍼)
- [ ] EditMode: FlowFieldCore, CellCenterToWorld, IsPassable, IsPassableForSize, BuildPassabilityMap (5종)
- [ ] PlayMode: 이동, 필드 공유, 동적 장애물(IsGridStale 포함), 벽 통과 차단(Small/Large 혼합), Flying, Partial Path, 캐시 무효화, 성능 (8종)
→ 상세: [07-Phase5-테스트.md](07-Phase5-테스트.md)

---

## 변경 파일 요약

| 작업 | 파일 |
|------|------|
| REWRITE | `Server/Systems/Movement/PathfindingSystem.cs` → FlowFieldSystem |
| REWRITE | `Server/Systems/Movement/PathFollowSystem.cs` → FlowFieldSteeringSystem |
| NEW | `Shared/Utilities/FlowFieldCore.cs` |
| NEW | `Server/Systems/Movement/GridObstacleResponseSystem.cs` |
| NEW | `Server/Systems/Movement/GridObstacleCleanupSystem.cs` |
| NEW | `Shared/Components/Movement/GridPathfindingSize.cs` |
| NEW | `Shared/Components/Movement/FlowFieldRef.cs` |
| NEW | `Shared/Components/Data/GridObstacleCleanup.cs` |
| NEW | `Shared/Singletons/FlowFieldCacheData.cs` |
| MODIFY | `Shared/Utilities/GridUtility.cs` |
| MODIFY | `Authoring/Movement/MovementAuthoring.cs` |
| MODIFY | `Shared/Systems/Grid/ObstacleGridInitSystem.cs` |
| MODIFY | `Shared/Components/Data/NavMeshObstacleProxy.cs` |
| MODIFY | `Shared/Components/Movement/MovementGoal.cs` (필드 제거 + bool `[MarshalAs]` 확인) |
| MODIFY | `Server/Systems/Movement/PredictedMovementSystem.cs` (attribute만) |
| MODIFY | `Server/Systems/Combat/UnifiedTargetingSystem.cs` (attribute만) |
| MODIFY | `Server/Systems/Combat/MeleeAttackSystem.cs` |
| MODIFY | `Server/Systems/Combat/RangedAttackSystem.cs` (2곳: 183행, 261행) |
| MODIFY | `Server/Systems/Commands/Combat/HandleAttackRequestSystem.cs` |
| MODIFY | `Server/Systems/Commands/Movement/HandleMoveRequestSystem.cs` |
| MODIFY | `Server/Systems/Commands/Construction/HandleBuildMoveRequestSystem.cs` |
| MODIFY | `Server/Systems/Commands/Construction/BuildArrivalSystem.cs` |
| MODIFY | `Authoring/Entities/StructureAuthoring.cs` (Header/Tooltip 주석 갱신) |
| MODIFY | `Authoring/Entities/ResourceNodeAuthoring.cs` (주석 갱신) |
| MODIFY | `Authoring/Entities/EnemyAuthoring.cs` (주석 갱신) |
| MODIFY | `Authoring/Entities/UnitAuthoring.cs` (주석 갱신) |
| MODIFY | `Shared/Components/Stats/StructureFootprint.cs` (주석 갱신) |
| REMOVE | `Server/Systems/Movement/NavMeshObstacleSpawnSystem.cs` |
| REMOVE | `Server/Systems/Movement/NavMeshObstacleCleanupSystem.cs` |
| REMOVE | `Server/Utilities/NavMeshPathUtils.cs` |
| REMOVE | `Shared/Components/Movement/NavMeshAgentConfig.cs` |
| REMOVE | `Shared/Buffers/PathWaypoint.cs` |

**변경 없음**: MovementArrivalSystem, GridOccupancyEventSystem, BuildingUtility, HandleGatherRequestSystem, HandleReturnResourceRequestSystem, WorkerGatheringSystem

---

## 후속 필요 사항 (구현 후)

- 문서 업데이트: `Docs/Systems/엔티티 이동 시스템(navmesh).md`, `Docs/Systems/시스템 그룹 및 의존성.md`, `Docs/Systems/코드베이스 구조.md`
- `EnemyAuthoring.cs`, `UnitAuthoring.cs`의 NavMeshAgentConfig/PathWaypoint 관련 주석 정리
- `NeedsNavMeshObstacle` → `NeedsGridObstacle` 리네이밍 (영향 파일 8개+, 안정화 후 별도 리팩토링)
- `NavMeshObstacleProxy.cs` 파일명 → `GridObstacleTag.cs` 등으로 변경
- `com.unity.ai.navigation` 패키지 제거: 모든 `using UnityEngine.AI`, `using UnityEngine.Experimental.AI`가 코드베이스에서 제거되었음을 확인 후 패키지 제거
- `InGame.unity` 씬의 NavMesh 관련 설정 (`m_IgnoreNavMeshObstacle` 등) 정리 확인

---

## 성능 비교

| 항목 | NavMesh (현재) | Flow Field (전환 후) |
|------|---------------|---------------------|
| 100유닛 동일 목적지 | NavMeshQuery x 100회 | BFS x **1회** |
| 계산 단위 | **유닛당** (N유닛 = N회) | **목적지당** (동일 목적지 공유) |
| managed 객체 | 건물당 NavMeshObstacle GO | **0** |
| 장애물 반영 지연 | carving 0.5초 | **즉시** (GridCell 직접 참조) |
| 데이터 소스 | NavMesh + Grid + Physics (3중) | **Grid + Physics (2중)** |
| 캐시 메모리 | NavMesh bake (~수 MB) | 32 필드 x 10KB = **320KB** |
