# VAT 애니메이션 + 사운드 시스템 — 통합 체크리스트

## Context

Project-SOS는 수천 유닛이 동시에 활동하는 DOTS 기반 RTS 게임이다. 현재 애니메이션과 사운드가 전혀 구현되어 있지 않다 (정적 MeshRenderer만 사용). 수천 마리 규모를 처리해야 하므로 Hybrid Animator(MonoBehaviour) 방식은 성능상 불가능하며, **GPU Animation (Vertex Animation Texture)** 방식이 필수다.

**전제 조건**: 스켈레탈 애니메이션이 포함된 FBX 모델이 필요하다. 현재 게임 유닛(Hero, Worker, Striker 등)은 정적 메시만 사용 중이므로, 애니메이션 포함 모델 확보가 선행되어야 한다.

---

## VAT 개요

스켈레탈 애니메이션을 텍스처에 베이킹하여, 런타임에서 셰이더가 텍스처 룩업만으로 버텍스 위치를 결정하는 방식. Animator/SkinnedMeshRenderer를 완전히 제거하고, 일반 MeshRenderer + 커스텀 셰이더로 수천 엔티티를 GPU에서 동시에 애니메이팅한다.

---

## 네트워크 전략

- **유닛/적 공통**: `VATAnimationState` (Ghost 동기화, ~3바이트/엔티티)
- 서버가 UnitActionState/EnemyState 변화 시 VATAnimationState를 갱신
- Ghost Relevancy로 뷰포트 밖 엔티티는 이미 필터링되므로 대역폭 영향 미미
- PlaybackSystem이 단일 쿼리로 통합 가능 (유닛/적 분기 불필요)

---

## SystemGroup 배치

```
[SimulationSystemGroup - Server]
  ... (기존 시스템들) ...
  [FixedStepSimulationSystemGroup]
    MeleeAttackSystem → RangedAttackSystem → DamageApplySystem
      ↓ UpdateAfter(typeof(FixedStepSimulationSystemGroup))
  VATAnimationStateUpdateSystem (유닛+적 애니메이션 상태 갱신)

[SimulationSystemGroup - Client]
  VATAnimationInitSystem (새 메시 엔티티 초기화)
    ↓ UpdateBefore
  VATAnimationPlaybackSystem (애니메이션 재생 → VATAnimParam 갱신)
    ↓ UpdateAfter
  SoundEventEmitSystem (상태 변화 → 사운드 이벤트)
    ↓ (기존)
  TeamColorSystem (팀 색상 - 기존 유지)
```

---

## Phase 체크리스트

### Phase 1: VAT 셰이더 + ECS 컴포넌트 정의
- [ ] URP Lit 기반 `VATAnimation.shader` 작성 (`_VATPositionTex`, `_VATTexelSize` 프로퍼티)
- [ ] `_VATAnimParam` float4 per-entity 프로퍼티 + UV2.x 버텍스 인덱스 읽기
- [ ] 프레임 간 보간 (floor/ceil + lerp) + `_BaseColor` 유지 (TeamColorSystem 호환)
- [ ] `VATAnimationState` (Shared, GhostField: CurrentClipIndex(byte) + AnimStartTime(float, Quantization=100))
- [ ] `VATClipBlobData` + `VATClipInfo` BlobAsset 구조 + `VATClipLibrary` 컴포넌트
- [ ] `VATClipDataAsset` ScriptableObject (PositionTexture + StaticMesh + ClipEntry[])
- [ ] `VATAnimParam` MaterialProperty 컴포넌트 (Client)
- [ ] `VATAnimTarget` 컴포넌트 (Client, 메시→루트 참조)
- [ ] `PreviousClipIndex` 컴포넌트 (Client, 사운드 변화 감지용)
→ 상세: [01-Phase1-셰이더및컴포넌트.md](01-Phase1-셰이더및컴포넌트.md)

### Phase 2: VAT 에디터 베이킹 툴
- [ ] `VATBakerWindow.cs` EditorWindow UI (FBX 슬롯, 클립 자동 추출, Fps/Loop 편집, 진행률 표시)
- [ ] `VATBakeUtility.cs` 베이킹 로직 (AnimationMode API → BakeMesh(useScale:false) → NativeArray<half4>)
- [ ] Position Texture (RGBAHalf) 생성 — `GetRawTextureData<half4>()` 직접 쓰기
- [ ] Static Mesh 추출 (바인드포즈) + UV2.x 버텍스 인덱스 인코딩 (정규화 금지)
- [ ] VATClipDataAsset 자동 생성 (StartRow, RowCount, Fps, Loop + 텍스처/메시 참조)
- [ ] VAT Material 자동 생성 (`_VATPositionTex` + `_VATTexelSize` 자동 설정)
- [ ] 파일 저장 경로: `Assets/VATData/{모델명}/` (4개 에셋)
- [ ] 텍스처 크기 한계(4096) 검증 + 에러 핸들링
→ 상세: [02-Phase2-베이킹툴.md](02-Phase2-베이킹툴.md)

### Phase 3: ECS 시스템 (서버 + 클라이언트)
- [ ] `VATAnimationStateUpdateSystem` (서버) — Action/EnemyContext → clip index (switch expression)
- [ ] 유닛 클립 매핑 (Idle→0, Moving→1, Working→2, Attacking→3, Dying→4, Dead→5, Disabled→6)
- [ ] 적 클립 매핑 (Idle/Dormant→0, Wandering/Chasing→1, Attacking→2, Dying→3, Dead→4, Disabled→5)
- [ ] 변화 감지: 이전 CurrentClipIndex와 비교, 변화 시에만 갱신
- [ ] Dying/Dead 상태 주의: ClientDeathSystem 즉시 렌더링 비활성화 → Dying 애니메이션 미재생 가능
- [ ] `VATAnimationInitSystem` (클라이언트) — `WithAll<MaterialMeshInfo>` + `WithNone<VATAnimParam>` 쿼리
- [ ] `VATAnimationPlaybackSystem` (클라이언트, IJobEntity) — normalizedTime/clipStartRow/clipRowCount 계산
- [ ] 진행률 계산: 루프=fmod, 비루프=saturate
- [ ] clipIndex 바운드 보호: `math.clamp(clipIndex, 0, Clips.Length - 1)`
- [ ] 시스템 의존성 설정 (UpdateInGroup, UpdateAfter, UpdateBefore, WorldSystemFilter)
→ 상세: [03-Phase3-ECS시스템.md](03-Phase3-ECS시스템.md)

### Phase 4: Authoring + 프리팹 통합
- [ ] Phase 2 출력물 존재 확인 (`Assets/VATData/{모델명}/` 내 4개 에셋)
- [ ] `VATAnimationAuthoring.cs` 구현 (Baker: BlobBuilder → VATClipBlobData → VATClipLibrary)
- [ ] Baker: ClipData null 시 `Debug.LogError` 진단 + 조기 반환
- [ ] Baker에서 VATAnimationState + VATClipLibrary 부착
- [ ] Phase 2에서 자동 생성된 VAT 머티리얼 확인 (`_VATPositionTex` + `_VATTexelSize`)
- [ ] 유닛/적 프리팹 8개에 VATAnimationAuthoring 추가 (ClipData 참조 + Inspector 확인)
- [ ] 프리팹 MeshFilter → VAT 정적 메시(`_BindPose.asset`)로 교체
- [ ] 프리팹 MeshRenderer 머티리얼 → VAT 머티리얼(`_VAT.mat`)로 교체
- [ ] 기존 UnitAuthoring/EnemyAuthoring 수정 불필요 확인 (Composition 패턴)
- [ ] Bake 실행 후 에러 메시지 없음 확인
→ 상세: [04-Phase4-Authoring통합.md](04-Phase4-Authoring통합.md)

### Phase 5: 사운드 시스템 (설계 + 구조)
- [ ] `SoundType` enum (Shared, byte, MeleeHit/RangedShot/UnitDeath/EnemyDeath/WorkerGather 등)
- [ ] `SoundEvent` IBufferElementData (`[InternalBufferCapacity(8)]`) + `SoundEventState` 싱글톤 (Client)
- [ ] `SoundEventEmitSystem` (Client, `[BurstCompile]`) — PreviousClipIndex vs CurrentClipIndex 비교 → SoundEvent 생성
- [ ] SoundEvent.Position: `LocalToWorld.Position` 사용
- [ ] 유닛/적 구분: `UnitTag`/`EnemyTag` Lookup → 클립 인덱스별 다른 SoundType 매핑
- [ ] 클립 전환 → SoundType 매핑 규칙 (근접/원거리 구분: RangedUnitTag/RangedEnemyTag)
- [ ] `SoundManager` MonoBehaviour — AudioSource 풀(32개), 3D 오디오 설정, 거리 컬링(80m), 동시 재생 제한(3개)
- [ ] 버퍼 Clear 타이밍: SoundManager.Update() 마지막에 수행
- [ ] `ClientBootstrapSystem` 수정 — SoundEventState 싱글톤 + SoundEvent 버퍼 생성
→ 상세: [05-Phase5-사운드시스템.md](05-Phase5-사운드시스템.md)

### Phase 6: 사운드 에셋 통합 + 문서 업데이트
- [ ] SoundManager에 SoundType → AudioClip 매핑 할당 (Inspector)
- [ ] 각 SoundType별 AudioClip 재생 검증 + 볼륨 기본값 설정
- [ ] 파라미터 튜닝: 풀 크기(32), 컬링 거리(80m), 동시 재생 제한(3개)
- [ ] Hero 전체 상태 사이클 테스트 (Idle → Moving → Attacking → Dying → Dead)
- [ ] 500+ 유닛 전투 시 사운드 성능 프로파일링
- [ ] `Docs/Architecture.md` — System Execution Flow + Key Patterns + Folder Structure 추가
- [ ] `Docs/Systems/코드베이스 구조.md` — 신규 파일 목록 반영
- [ ] 주석 정합성 점검 (Phase 1~5 변경 파일)
- [ ] CLAUDE.md 동기화 (VAT/사운드 패턴 추가 여부 검토)
- [ ] WorkLog 기록 (`Docs/WorkLog/YYYY-MM-DD/`)
→ 상세: [06-Phase6-사운드에셋및문서.md](06-Phase6-사운드에셋및문서.md)

---

## 변경 파일 요약

### 신규 생성
```
Assets/
├── Editor/VATBaker/
│   ├── VATBakerWindow.cs
│   └── VATBakeUtility.cs
├── Scripts/
│   ├── Shared/
│   │   ├── Components/Animation/VATAnimationState.cs
│   │   ├── Components/Animation/VATClipLibrary.cs
│   │   ├── Components/Sound/SoundType.cs
│   │   └── Components/Animation/VATClipDataAsset.cs
│   ├── Client/
│   │   ├── Component/Animation/VATAnimParam.cs
│   │   ├── Component/Animation/VATAnimTarget.cs
│   │   ├── Component/Animation/PreviousClipIndex.cs
│   │   ├── Component/Sound/SoundEvent.cs
│   │   ├── Component/Singleton/SoundEventState.cs
│   │   ├── Systems/Animation/VATAnimationInitSystem.cs
│   │   ├── Systems/Animation/VATAnimationPlaybackSystem.cs
│   │   ├── Systems/Sound/SoundEventEmitSystem.cs
│   │   └── Controller/Sound/SoundManager.cs
│   ├── Server/Systems/Animation/VATAnimationStateUpdateSystem.cs
│   └── Authoring/Animation/VATAnimationAuthoring.cs
├── Shaders/VATAnimation.shader
└── VATData/ (베이킹 출력: *_Positions.asset, *_BindPose.asset, *_ClipData.asset, *_VAT.mat)
```

### 기존 수정
| 파일 | 변경 |
|------|------|
| `ClientBootstrapSystem.cs` | SoundEventState 싱글톤 초기화 + SoundEvent 버퍼 추가 |
| 유닛/적 프리팹 8개 | VATAnimationAuthoring 추가, 머티리얼 교체 |

---

## 검증 방법

1. **VAT 베이킹**: Editor 툴에서 테스트 FBX → Position Texture + ClipData 생성 확인
2. **셰이더**: VAT 머티리얼 적용한 정적 메시에서 VATAnimParam 수동 변경 → 버텍스 변형 확인
3. **ECS 통합**: Hero 프리팹 1개로 파일럿 테스트 → Idle/Moving/Attacking 전환 시 애니메이션 재생 확인
4. **대규모 테스트**: 500+ 유닛 스폰 → 프로파일러에서 GPU/CPU 부하 확인
5. **네트워크**: 클라이언트-서버 모드에서 적 애니메이션 동기화 확인
6. **사운드**: SoundManager에 테스트 AudioClip 할당 → 전투 시 사운드 재생 확인

---

## 주요 기술적 고려사항

- **UV2 인코딩**: SV_VertexID 대신 베이킹 시 UV2에 버텍스 인덱스를 인코딩 (Entities Graphics 호환성). 정규화 금지 — 정수값 그대로 기록 (`uv2[i] = new Vector2((float)i, 0)`)
- **RGBAHalf 텍스처 쓰기**: `SetPixels()` 사용 불가 → `GetRawTextureData<half4>()`로 NativeArray 직접 조작. Half(16bit)로 시작, 정밀도 부족 시 Float(32bit)로 전환
- **AnimationMode API**: Editor에서 AnimationClip 샘플링 시 `AnimationMode.StartAnimationMode()` → `SampleAnimation()` → `BakeMesh(useScale:false)` → `StopAnimationMode()` 필수
- **텍스처 크기 한계**: 너비/높이 4096 초과 시 분할 필요. 프레임 수 = `round(clipLength × fps)`로 정확히 산출
- **프레임 보간**: 셰이더에서 2프레임 lerp로 끊김 방지
- **bool blittable**: VATClipInfo.Loop 필드는 BlobAsset 내부 읽기전용이므로 MarshalAs 불필요
- **TeamColorSystem 호환**: VATAnimParam과 URPMaterialPropertyBaseColor는 독립 초기화 (충돌 없음)
- **LOD**: Phase 1에서는 미구현, 프로파일링 후 필요 시 추가
- **Dying/Dead 상태 처리**: `ClientDeathSystem`이 사망 엔티티의 렌더링을 즉시 비활성화하므로, Dying 애니메이션은 실제 재생되지 않을 가능성이 높음. 클립 인덱스 매핑은 유지하되 (폴백 안전성), 사망 연출이 필요하면 ClientDeathSystem 수정 선행 필요. 베이킹 시 Dying/Dead 클립 누락 시 `math.clamp`로 바운드 보호됨
- **SoundEvent.Position**: 엔티티의 `LocalToWorld.Position` 사용. 버퍼 Clear는 SoundManager.Update() 마지막에 수행
- **3D AudioSource**: `spatialBlend=1.0`, `minDistance=5`, `maxDistance=80`, `rolloffMode=Linear`

---

## 핵심 참조 파일

- `Client/Systems/TeamColorSystem.cs` - 자식 메시 엔티티 초기화 패턴 (Parent 체인 탐색 + ECB)
- `Shared/Components/State/UnitActionState.cs` - Action enum (Idle/Moving/Working/Attacking/Dying/Dead)
- `Shared/Components/State/EnemyState.cs` - EnemyContext enum (Idle/Wandering/Attacking/Chasing/Dormant/Dying/Dead)
- `Client/Systems/Initialize/ClientBootstrapSystem.cs` - 싱글톤 초기화 패턴
