# VAT 애니메이션 + 사운드 시스템 — 통합 체크리스트

## Context

Project-SOS는 수천 유닛이 동시에 활동하는 DOTS 기반 RTS 게임이다. 현재 애니메이션과 사운드가 전혀 구현되어 있지 않다 (정적 MeshRenderer만 사용). 수천 마리 규모를 처리해야 하므로 Hybrid Animator(MonoBehaviour) 방식은 성능상 불가능하며, **GPU Animation (Vertex Animation Texture)** 방식이 필수다.

**전제 조건**: 스켈레탈 애니메이션이 포함된 FBX 모델이 필요하다. 에셋 조사 결과 VAT 적용 가능한 프리팹은 Hero(LittleSquirrel), EnemySmall/EnemyFlying(Ghost), EnemyBig(Little_Ghost_ZOMBANGEL) **4종**이며, Origami 유닛 4종(Worker/Striker/Tank/Archer)은 리깅 부재로 VAT 불가. EnemyBig은 idle_up_down 1클립만 있으나 정적 메시보다 생동감을 위해 적용. Hero는 Sleep02를 사망 애니메이션으로 대체 활용. 전투 연출은 유닛 기울임(Rotation pitch)으로 보완한다. 상세: [00-에셋조사.md](00-에셋조사.md)

---

## VAT 개요

스켈레탈 애니메이션을 텍스처에 베이킹하여, 런타임에서 셰이더가 텍스처 룩업만으로 버텍스 위치를 결정하는 방식. Animator/SkinnedMeshRenderer를 완전히 제거하고, 일반 MeshRenderer + 커스텀 셰이더로 수천 엔티티를 GPU에서 동시에 애니메이팅한다.

---

## 네트워크 전략

- **유닛/적 공통**: `VATAnimationState` (Ghost 동기화, ~3바이트/엔티티)
- 서버가 UnitActionState/EnemyState 변화 시 VATAnimationState를 갱신
- Ghost Relevancy로 뷰포트 밖 엔티티는 이미 필터링되므로 대역폭 영향 미미
- PlaybackSystem이 단일 쿼리로 통합 가능 (유닛/적 분기 불필요)

---

## SystemGroup 배치

```
[SimulationSystemGroup - Server]
  ... (기존 시스템들) ...
  [FixedStepSimulationSystemGroup]
    MeleeAttackSystem → RangedAttackSystem → DamageApplySystem
      ↓ UpdateAfter(typeof(FixedStepSimulationSystemGroup))
  VATAnimationStateUpdateSystem (VAT 적용 유닛+적 애니메이션 상태 갱신)

[SimulationSystemGroup - Client]
  VATAnimationInitSystem (새 메시 엔티티 초기화, VAT 대상만)
    ↓ UpdateBefore
  VATAnimationPlaybackSystem (애니메이션 재생 → VATAnimParam 갱신)
    ↓ UpdateAfter
  CombatTiltSystem (전투 기울임, 전체 유닛/적 대상 — VAT 유무 무관)
    ↓ UpdateAfter
  SoundEventEmitSystem (UnitActionState/EnemyState 변화 → 사운드 이벤트, 전체 대상)
    ↓ (기존)
  TeamColorSystem (팀 색상 - 기존 유지)
```

---

## Phase 체크리스트

### Phase 1: VAT 셰이더 + ECS 컴포넌트 정의 --- Completed
- [x] URP Lit 기반 `VATAnimation.shader` 작성 (`_VATPositionTex`, `_VATTexelSize` 프로퍼티)
- [x] `_VATAnimParam` float4 per-entity 프로퍼티 + UV2.x 버텍스 인덱스 읽기
- [x] 프레임 간 보간 (floor/ceil + lerp) + `_BaseColor` 유지 (TeamColorSystem 호환)
- [x] `VATAnimationState` (Shared, GhostField: CurrentClipIndex(byte) + AnimStartTime(float, Quantization=100))
- [x] `VATClipBlobData` + `VATClipInfo` BlobAsset 구조 + `VATClipLibrary` 컴포넌트
- [x] `VATClipDataAsset` ScriptableObject (PositionTexture + StaticMesh + ClipEntry[])
- [x] `VATAnimParam` MaterialProperty 컴포넌트 (Client)
- [x] `VATAnimTarget` 컴포넌트 (Client, 메시→루트 참조)
- [x] `PreviousClipIndex` 컴포넌트 (Client, VAT 클립 전환 감지용 — 사운드는 Phase 5에서 PreviousActionState/PreviousEnemyContext 사용)
→ 상세: [01-Phase1-셰이더및컴포넌트.md](01-Phase1-셰이더및컴포넌트.md)

### Phase 2: VAT 에디터 베이킹 툴 --- Completed
- [x] `VATBakerWindow.cs` EditorWindow UI (FBX 슬롯, 클립 자동 추출, Fps/Loop 편집, 출력 경로, 진행률 표시)
- [x] `VATBakeUtility.cs` 베이킹 로직 (AnimationMode API → BakeMesh(useScale:false) → NativeArray<half4>)
- [x] Position Texture (RGBAHalf) 생성 — `GetRawTextureData<half4>()` 직접 쓰기
- [x] Static Mesh 추출 (바인드포즈) + UV2.x 버텍스 인덱스 인코딩 (정규화 금지)
- [x] VATClipDataAsset 자동 생성 (StartRow, RowCount, Fps, Loop + 텍스처/메시 참조)
- [x] VAT Material 자동 생성 (`_VATPositionTex` + `_VATTexelSize` 자동 설정)
- [x] 파일 저장 경로: `Assets/VATData/{모델명}/` (4개 에셋)
- [x] 텍스처 크기 한계(4096) 검증 + 에러 핸들링
→ 상세: [02-Phase2-베이킹툴.md](02-Phase2-베이킹툴.md)

### Phase 3: ECS 시스템 (서버 + 클라이언트) --- Completed
- [x] `VATAnimationStateUpdateSystem` (서버) — Action/EnemyContext → clip index (switch expression)
- [x] VAT 적용 대상: Hero, EnemySmall, EnemyFlying **3종** (VATAnimationState 보유 엔티티만 쿼리 대상)
- [x] 유닛 클립 매핑 — Hero(LittleSquirrel) 기준: Idle→0, Moving→1, Working→2, 나머지→0 폴백
- [x] 적 클립 매핑 — Ghost 기준: Idle→0, Wandering/Chasing→1, Attacking→2, Dying→3
- [x] VAT 미적용 유닛(Origami 4종 + EnemyBig): VATAnimationAuthoring 미부착 → 쿼리에서 자연 제외
- [x] 전투 기울임 시스템: UnitActionState/EnemyState 기반 Rotation pitch 조작 (VAT 유무와 무관, 전체 유닛/적 대상)
- [x] 변화 감지: 이전 CurrentClipIndex와 비교, 변화 시에만 갱신
- [x] `VATAnimationInitSystem` (클라이언트) — `WithAll<MaterialMeshInfo>` + `WithNone<VATAnimParam>` 쿼리
- [x] `VATAnimationPlaybackSystem` (클라이언트, IJobEntity) — normalizedTime/clipStartRow/clipRowCount 계산
- [x] 진행률 계산: 루프=fmod, 비루프=saturate
- [x] clipIndex 바운드 보호: `math.clamp(clipIndex, 0, Clips.Length - 1)`
- [x] 시스템 의존성 설정 (UpdateInGroup, UpdateAfter, UpdateBefore, WorldSystemFilter)
→ 상세: [03-Phase3-ECS시스템.md](03-Phase3-ECS시스템.md)

### Phase 4: Authoring + 프리팹 통합 --- Completed
- [x] Phase 2 출력물 존재 확인 (`Assets/VATData/{모델명}/` 내 에셋)
- [x] `VATAnimationAuthoring.cs` 구현 (Baker: BlobBuilder → VATClipBlobData → VATClipLibrary)
- [x] Baker: ClipData null 시 `Debug.LogError` 진단 + 조기 반환
- [x] Baker에서 VATAnimationState + VATClipLibrary 부착
- [x] Phase 2에서 자동 생성된 VAT 머티리얼 확인 (`_VATPositionTex` + `_VATTexelSize`)
- [x] VAT 적용 프리팹 **3개**에 VATAnimationAuthoring 추가: Hero, EnemySmall, EnemyFlying
- [x] 각 프리팹의 Inspector에서 ClipData 할당 확인
- [x] VAT 미적용 프리팹 5개(Worker/Striker/Tank/Archer/EnemyBig)는 기존 정적 메시 유지
- [x] 프리팹 MeshFilter → VAT 정적 메시(`_BindPose.asset`)로 교체 (3개)
- [x] 프리팹 MeshRenderer 머티리얼 → VAT 머티리얼(`_VAT.mat`)로 교체 (3개)
- [x] 기존 UnitAuthoring/EnemyAuthoring 수정 불필요 확인 (Composition 패턴)
- [x] Bake 실행 후 에러 메시지 없음 확인
→ 상세: [04-Phase4-Authoring통합.md](04-Phase4-Authoring통합.md)

### Phase 5: 사운드 시스템 (설계 + 구조) --- Completed
- [x] `SoundType` enum (Shared, byte, MeleeHit/RangedShot/UnitDeath/EnemyDeath/WorkerGather 등)
- [x] `SoundEvent` IBufferElementData (`[InternalBufferCapacity(8)]`) + `SoundEventState` 싱글톤 (Client)
- [x] `SoundEventEmitSystem` (Client, `[BurstCompile]`) — **UnitActionState/EnemyState 직접 감지** → SoundEvent 생성
- [x] VAT 유무와 무관하게 전체 유닛/적에서 사운드 이벤트 발생 (VATAnimTarget 의존 제거)
- [x] `PreviousActionState`/`PreviousEnemyContext` 컴포넌트로 상태 변화 감지
- [x] SoundEvent.Position: `LocalToWorld.Position` 사용
- [x] Action/EnemyContext → SoundType 매핑 (근접/원거리 구분: RangedUnitTag/RangedEnemyTag)
- [x] `SoundManager` MonoBehaviour — AudioSource 풀(32개), 3D 오디오 설정, 거리 컬링(80m), 동시 재생 제한(3개)
- [x] 버퍼 Clear 타이밍: SoundManager.Update() 마지막에 수행
- [x] `ClientBootstrapSystem` 수정 — SoundEventState 싱글톤 + SoundEvent 버퍼 생성
→ 상세: [05-Phase5-사운드시스템.md](05-Phase5-사운드시스템.md)

### Phase 6: 사운드 에셋 통합 + 문서 업데이트
- [ ] SoundManager에 SoundType → AudioClip 매핑 할당 (Inspector)
- [ ] 각 SoundType별 AudioClip 재생 검증 + 볼륨 기본값 설정
- [ ] 파라미터 튜닝: 풀 크기(32), 컬링 거리(80m), 동시 재생 제한(3개)
- [ ] VAT 유닛 테스트: Hero Idle→Moving→Working (걷기 VAT), Attacking (기울임 폴백)
- [ ] VAT 적 테스트: EnemySmall Idle→Moving→Attacking→Dying (VAT + 기울임)
- [ ] VAT 적 테스트: EnemyFlying — EnemySmall과 동일 동작 확인 (같은 FBX)
- [ ] 비VAT 유닛 테스트: Worker/Striker/Tank/Archer Idle→Working→Attacking (기울임 + 사운드)
- [ ] 500+ 유닛 전투 시 사운드 성능 프로파일링
- [ ] `Docs/Architecture.md` — System Execution Flow + Key Patterns + Folder Structure 추가
- [ ] `Docs/Systems/코드베이스 구조.md` — 신규 파일 목록 반영
- [ ] 주석 정합성 점검 (Phase 1~5 변경 파일)
- [ ] CLAUDE.md 동기화 (VAT/사운드 패턴 추가 여부 검토)
- [ ] WorkLog 기록 (`Docs/WorkLog/YYYY-MM-DD/`)
→ 상세: [06-Phase6-사운드에셋및문서.md](06-Phase6-사운드에셋및문서.md)

---

## 변경 파일 요약

### 신규 생성
```
Assets/
├── Editor/VATBaker/
│   ├── VATBakerWindow.cs
│   └── VATBakeUtility.cs
├── Scripts/
│   ├── Shared/
│   │   ├── Components/Animation/VATAnimationState.cs
│   │   ├── Components/Animation/VATClipLibrary.cs
│   │   ├── Components/Sound/SoundType.cs
│   │   └── Components/Animation/VATClipDataAsset.cs
│   ├── Client/
│   │   ├── Component/Animation/VATAnimParam.cs
│   │   ├── Component/Animation/VATAnimTarget.cs
│   │   ├── Component/Animation/PreviousClipIndex.cs
│   │   ├── Component/Sound/SoundEvent.cs
│   │   ├── Component/Singleton/SoundEventState.cs
│   │   ├── Component/State/PreviousActionState.cs
│   │   ├── Component/State/PreviousEnemyContext.cs
│   │   ├── Systems/Animation/VATAnimationInitSystem.cs
│   │   ├── Systems/Animation/VATAnimationPlaybackSystem.cs
│   │   ├── Systems/Animation/CombatTiltSystem.cs
│   │   ├── Systems/Sound/SoundEventEmitSystem.cs
│   │   └── Controller/Sound/SoundManager.cs
│   ├── Server/Systems/Animation/VATAnimationStateUpdateSystem.cs
│   └── Authoring/Animation/VATAnimationAuthoring.cs
├── Shaders/VATAnimation.shader
└── VATData/ (베이킹 출력: *_Positions.asset, *_BindPose.asset, *_ClipData.asset, *_VAT.mat)
```

### 기존 수정
| 파일 | 변경 |
|------|------|
| `ClientBootstrapSystem.cs` | SoundEventState 싱글톤 초기화 + SoundEvent 버퍼 추가 |
| `GameSettings` / `GameSettingsAuthoring` | CombatTiltSystem용 tiltAngle, tiltSpeed 필드 추가 |
| VAT 적용 프리팹 4개 (Hero, EnemySmall, EnemyFlying, EnemyBig) | VATAnimationAuthoring 추가, 메시/머티리얼 교체 |
| VAT 미적용 프리팹 4개 (Worker, Striker, Tank, Archer) | 변경 없음 (기존 정적 메시 유지) |

---

## 검증 방법

1. **VAT 베이킹**: Editor 툴에서 Ghost FBX / LittleSquirrel FBX → Position Texture + ClipData 생성 확인
2. **셰이더**: VAT 머티리얼 적용한 정적 메시에서 VATAnimParam 수동 변경 → 버텍스 변형 확인
3. **ECS 통합 (VAT)**: Hero 프리팹으로 Idle/Moving/Working 전환 시 걷기 애니메이션 재생 확인
4. **ECS 통합 (기울임)**: Origami 유닛(Worker 등)으로 Attacking 시 기울임 동작 확인
5. **대규모 테스트**: 500+ 유닛 스폰 → 프로파일러에서 GPU/CPU 부하 확인
6. **네트워크**: 클라이언트-서버 모드에서 적 애니메이션 동기화 확인
7. **사운드**: SoundManager에 테스트 AudioClip 할당 → 전투 시 사운드 재생 확인 (VAT 유무 무관)

---

## 주요 기술적 고려사항

- **UV2 인코딩**: SV_VertexID 대신 베이킹 시 UV2에 버텍스 인덱스를 인코딩 (Entities Graphics 호환성). 정규화 금지 — 정수값 그대로 기록 (`uv2[i] = new Vector2((float)i, 0)`)
- **RGBAHalf 텍스처 쓰기**: `SetPixels()` 사용 불가 → `GetRawTextureData<half4>()`로 NativeArray 직접 조작. Half(16bit)로 시작, 정밀도 부족 시 Float(32bit)로 전환
- **AnimationMode API**: Editor에서 AnimationClip 샘플링 시 `AnimationMode.StartAnimationMode()` → `SampleAnimation()` → `BakeMesh(useScale:false)` → `StopAnimationMode()` 필수
- **텍스처 크기 한계**: 너비/높이 4096 초과 시 분할 필요. 프레임 수 = `round(clipLength × fps)`로 정확히 산출
- **프레임 보간**: 셰이더에서 2프레임 lerp로 끊김 방지
- **bool blittable**: VATClipInfo.Loop 필드는 BlobAsset 내부 읽기전용이므로 MarshalAs 불필요
- **TeamColorSystem 호환**: VATAnimParam과 URPMaterialPropertyBaseColor는 독립 초기화 (충돌 없음)
- **LOD**: Phase 1에서는 미구현, 프로파일링 후 필요 시 추가
- **Dying/Dead 상태 처리**: `ClientDeathSystem`이 사망 엔티티의 렌더링을 즉시 비활성화하므로, Dying 애니메이션은 실제 재생되지 않을 가능성이 높음. 클립 인덱스 매핑은 유지하되 (폴백 안전성), 사망 연출이 필요하면 ClientDeathSystem 수정 선행 필요. 베이킹 시 Dying/Dead 클립 누락 시 `math.clamp`로 바운드 보호됨
- **SoundEvent.Position**: 엔티티의 `LocalToWorld.Position` 사용. 버퍼 Clear는 SoundManager.Update() 마지막에 수행
- **3D AudioSource**: `spatialBlend=1.0`, `minDistance=5`, `maxDistance=80`, `rolloffMode=Linear`

---

## 핵심 참조 파일

- `Client/Systems/TeamColorSystem.cs` - 자식 메시 엔티티 초기화 패턴 (Parent 체인 탐색 + ECB)
- `Shared/Components/State/UnitActionState.cs` - Action enum (Idle=0, Moving=1, Working=2, Attacking=3, Disabled=200, Dying=254, Dead=255)
- `Shared/Components/State/EnemyState.cs` - EnemyContext enum (Idle=0, Wandering=1, Attacking=2, Chasing=3, Dormant=10, Disabled=20, Dying=254, Dead=255)
- `Client/Systems/Initialize/ClientBootstrapSystem.cs` - 싱글톤 초기화 패턴
- `Client/Controller/UI/MinimapRenderer.cs` - World.All + `world.IsClient()` 패턴 (SoundManager 참조)
- `00-에셋조사.md` - 에셋별 애니메이션 상세 분석 + 프리팹-에셋 매핑
